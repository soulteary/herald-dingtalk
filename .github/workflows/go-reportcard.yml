name: Go Report Card

on:
  push:
    branches: [ main ]
    paths-ignore:
      - '.github/goreportcard.svg'
      - '.github/goreportcard-report.md'
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: go-report-card-${{ github.ref }}
  cancel-in-progress: true

jobs:
  report-card:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - id: grc
        uses: soulteary/goreportcard-action@v1.0.0
        with:
          directory: "."
          output: ".github/goreportcard.svg"
          report: "true"
          report_output: ".github/goreportcard-report.md"
          threshold: "90"
          commit: "true"
          version: "v1.0.0"
      - run: echo "Grade ${{ steps.grc.outputs.grade }} (${{ steps.grc.outputs.score }}%)"
